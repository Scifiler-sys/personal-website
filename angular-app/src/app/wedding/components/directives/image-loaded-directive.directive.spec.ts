import { ImageLoadedDirectiveDirective } from './image-loaded-directive.directive';

describe('ImageLoadedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
